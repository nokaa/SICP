; If b is greater than 0, the conditional returns the `+' function and applys a and b to it.
; If b is less than 0, the conditional returns the `-' function and applys a and b to it.
; This is effectively adding the absolute value of b to a.
